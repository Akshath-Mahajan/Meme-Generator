import { Typography } from '@material-ui/core'
import React from 'react'

function Loading() {
    return (
        <div className="loading">
            <Typography align="center" color='textPrimary' variant="h3">Loading</Typography>
        </div>
    )
}

export default Loading
